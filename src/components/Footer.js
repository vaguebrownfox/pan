import React from 'react'

// Styles
import classes from "../styles/Footer.module.css"

export const Footer = () => {
    return (
        <div className={classes.root}>
            <p className={classes.title}>{";{e.xit}"}</p>
        </div>
    )
}
