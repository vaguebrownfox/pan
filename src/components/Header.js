import React from 'react'

// Styles
import classes from "../styles/Header.module.css"

export const Header = () => {
    return (
        <div className={classes.root}>
            <h1 className={classes.title}>vaguebrownfox</h1>
        </div>
    )
}
